# Production Environment Variables for Render Deployment
# Copy this file to .env.production and fill in your actual values

# REQUIRED: Groq API Key
GROQ_API_KEY=your_groq_api_key_here

# Groq Configuration (optional - will use defaults if not set)
GROQ_MODEL=llama-3.1-8b-instant
GROQ_MAX_TOKENS=1000
GROQ_TEMPERATURE=0.7

# PostgreSQL Database (optional - will use memory fallback if not configured)
DB_HOST=your_render_postgres_host
DB_PORT=5432
DB_NAME=axiomos
DB_USER=your_render_postgres_user
DB_PASSWORD=your_render_postgres_password

# Redis (optional - will use memory fallback if not configured)
REDIS_HOST=your_render_redis_host
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=your_render_redis_password

# Production Settings
SESSION_TIMEOUT=3600
LOG_LEVEL=INFO

# Security Settings
CORS_ORIGINS=https://your-app-name.onrender.com

# Render-specific variables (automatically set by Render)
# PORT=10000
# PYTHON_VERSION=3.13